<div class="container mt-5">
  <h1>{{event.eventName}}</h1>

  <hr>

  <img src="{{event.eventImg}}" alt="event img">
  <p>Url: <a href="{{event.eventUrl}}">{{event.eventUrl}}</a></p>
  <p>Precio: {{event.price}}</p>
  <p>Ciudad: {{event.city}}</p>
  <p>Descripcion: {{event.description}}</p>


  <p>Creado por: {{event.createdBy.username}}</p>
  <form action="/detalles/{{event._id}}/favoritos" method="POST" style="display: inline">
    <input type="submit" value="Añadir a favoritos" class="btn btn-danger">
  </form>

  <hr>

  <a href="/our-events/events-list" class="btn btn-dark mb-5">Volver</a>

  {{#if isADMIN}}
  <a href="/detalles-evento/{{event._id}}/editar" class="btn btn-warning mb-5">Editar</a>
  <form action="/detalles-evento/{{event._id}}/eliminar/" method="POST" style="display: inline">
    <input type="submit" value="Eliminar" class="btn btn-danger mb-5">
  </form>

  {{/if}}

  {{#if isEDITOR}}
  <a href="/detalles-evento/{{event._id}}/editar" class="btn btn-warning mb-5">Editar</a>
  <form action="/detalles-evento/{{event._id}}/eliminar/" method="POST" style="display: inline">
    <input type="submit" value="Eliminar" class="btn btn-danger">
  </form>

  {{/if}}



</div>